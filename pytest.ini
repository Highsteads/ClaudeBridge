[pytest]
# Run bare from the repo root: `python3 -m pytest -q`. The suite tests the REPO
# bundle (tests/conftest.py); set CB_SP to point it at another Server Plugin
# folder, such as the installed copy.
testpaths = tests
norecursedirs = work drafts .git
