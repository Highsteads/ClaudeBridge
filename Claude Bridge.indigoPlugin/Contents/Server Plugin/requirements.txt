# Claude Bridge runtime dependencies.
# Indigo pip-installs this into Contents/Packages/ at plugin startup.
# Keep this list to packages the code actually imports — every entry is
# downloaded onto every user's machine. Transitive dependencies (requests,
# httpx, six, dateutil, ...) are resolved by pip and must NOT be listed here.

# Anthropic Claude API (startup connection test + completion calls)
anthropic>=0.40.0

# Schema models used by the LLM client
pydantic>=2.12.2

# Optional historical_analysis tools — legacy InfluxDB 1.x client
influxdb==5.3.2

# Prompt templating for historical analysis
jinja2>=3.1.0
